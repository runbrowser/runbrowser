{
  "name": "termio-browser-root",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter mcp-extension build",
    "build:extension": "pnpm --filter mcp-extension build",
    "dev:extension": "pnpm --filter mcp-extension dev",
    "typecheck": "pnpm --filter @termio/browser-server typecheck && pnpm --filter @termio/browser-mcp typecheck && pnpm --filter @termio/browser typecheck",
    "test": "pnpm --filter @termio/browser-server test",
    "publish:all": "pnpm --filter @termio/browser-server publish --no-git-checks && pnpm --filter @termio/browser-mcp publish --no-git-checks && pnpm --filter @termio/browser publish --no-git-checks"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
